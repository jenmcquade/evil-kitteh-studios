/* global twentyseventeenScreenReaderText */
(function( $ ) {

	// Reset so that margins aren't hard coded into header.
	function adjustHeaderHeight() {

	}

	// Fire on document ready.
	$( document ).ready( function() {

		adjustHeaderHeight();
		
	});

})( jQuery );
